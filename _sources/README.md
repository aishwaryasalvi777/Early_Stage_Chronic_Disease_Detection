# Jupyter Book Docs

## Build locally

1. Install Jupyter Book:
   pip install jupyter-book
2. Build the site from project root:
   jupyter-book build docs

The generated site will be available under `_build/html/`.
